# Rustfmt configuration
# Using stable features only

edition = "2021"
max_width = 100
tab_spaces = 4
newline_style = "Unix"
use_small_heuristics = "Default"

# Stable formatting options only
reorder_imports = true
reorder_modules = true
remove_nested_parens = true
